# dependencies
node_modules/

# build output
dist/

# secrets — never commit these. .env.example is the template.
.env
.env.*
!.env.example

# service account keys, if you download one instead of using env vars
*serviceAccount*.json
*firebase-adminsdk*.json

# logs, coverage, test artefacts
*.log
coverage/
.vitest/
playwright-report/
test-results/

# macOS
.DS_Store
